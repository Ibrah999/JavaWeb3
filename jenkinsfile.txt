pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the application'
                sh '/usr/share/maven/bin/mvn package'
            }
        }
        
        stage('Test') {
            steps {
                echo 'Running tests'
                sh '/usr/share/maven/bin/mvn test'
            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Deploying the application'
                
                // Deploying to Tomcat using the deploy step
                deploy adapters: [apache-tomcat-9.0.87/credentialsId: '', url: 'http://http://54.69.89.73:8080/], contextPath: null, war: '**/*.war'
                
                // Alternative: Deploying using SSH
                sshagent(['Deploy_user']) {
                    sh "scp -o StrictHostKeyChecking=no /var/lib/jenkins/workspace/jenkins/pipeline/target/webapp.war centos@172-31-28-122/home/centos/apache-tomcat-9.0.87webapps"
                }
            }
        }
    }
}

